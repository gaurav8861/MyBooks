package com.myBooks.util;

import com.myBooks.Exception.BookException;
import com.myBooks.model.dto.ProfileDTO;
import com.myBooks.serviceImpl.LoginService;

public class Test {
	
	/*public static void main(String[] args) {
		LoginService loginService = new LoginService();
		ProfileDTO profileDTO = new ProfileDTO();
		profileDTO.setProfileId(1);
		try {
			loginService.signUp(profileDTO); System.out.println("");
		} catch (BookException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
*/
}
